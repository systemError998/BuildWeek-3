import React from 'react'
import { Button } from 'react-bootstrap'

export const BtnDisponibileComponent = () => {
    return (
        <Button
            variant="primary"
            className='rounded-pill'
        >
            Disponibile per
        </Button>
    )
}
